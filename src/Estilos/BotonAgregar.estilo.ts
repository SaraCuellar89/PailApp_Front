import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boton: {
    position: "absolute",
    bottom: 0,
    width: "100%",
    backgroundColor: "#FFD600",
    alignItems: "center",
    padding: 10,
  },
  texto: {
    fontSize: 40,
  },
});